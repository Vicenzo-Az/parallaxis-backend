"""
Rotas do bounded context `games`, incluídas em config/urls.py sob /api/.

A implementar conforme as views forem criadas em views.py.
"""
# from django.urls import path

app_name = "games"

urlpatterns = [
    # path("games/search/", GameSearchView.as_view(), name="game-search"),
    # path("analysis/taste/", TasteAnalysisView.as_view(), name="taste-analysis"),
]
